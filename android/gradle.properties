org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
kotlin.code.style=official

# Release signing reads these, and they are NOT in the repository: see
# keystore.properties.example. A debug build needs none of them.
